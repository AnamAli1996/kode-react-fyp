/**
 * Test case for ap-tag.
 * Runs with mocha.
 */
'use strict'

import ApTag from '../lib/ap_tag'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('ap-tag', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <ApTag/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
