/**
 * menu for header.
 * @class ApHeaderMenu
 */

'use strict'

import React, {Component, PropTypes as types} from 'react'
import classnames from 'classnames'

/** @lends ApHeaderMenu */
class ApHeaderMenu extends Component {
  render () {
    const s = this
    let { props } = s
    return (
      <div className={ classnames('ap-header-menu', props.className) }>
        { props.children }
      </div>
    )
  }
}

Object.assign(ApHeaderMenu, {
  // --------------------
  // Specs
  // --------------------

  propTypes: {},

  defaultProps: {}
})

export default ApHeaderMenu
