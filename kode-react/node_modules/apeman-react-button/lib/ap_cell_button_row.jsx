/**
 * Row for Cell buttons.
 * @class ApCellButtonRow
 */

'use strict'

import React, {PropTypes as types} from 'react'
import classnames from 'classnames'

/** @lends ApCellButtonRow */
const ApCellButtonRow = React.createClass({

  // --------------------
  // Specs
  // --------------------

  propTypes: {},

  getInitialState () {
    return {}
  },

  getDefaultProps () {
    return {}
  },

  render () {
    const s = this
    let { props } = s
    return (
      <div className={ classnames('ap-cell-button-row', props.className) }>
        { props.children }
      </div>
    )
  }

})

export default ApCellButtonRow
