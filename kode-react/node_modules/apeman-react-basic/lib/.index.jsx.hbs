
/**
 * {{pkg.description}}
 * @namespace {{camelcase pkg.name}}
{{#each components}}
{{#each classes}}
 * @property {function} {{this}} - {{this}} from "{{../module}}" module.
{{/each}}
{{/each}}
*/

'use strict'

{{#each components}}
import {
{{#each classes}}
  {{this}}{{#if @last}}{{else}},
  {{/if}}
{{/each}}

} from '{{module}}'

{{/each}}

/** Package version */
const version = '{{{pkg.version}}}'

export {
{{#each components}}
  // --------------------------
  // {{module}}
  // --------------------------
{{#each classes}}
  {{this}},
{{/each}}
{{/each}}
  version
}
