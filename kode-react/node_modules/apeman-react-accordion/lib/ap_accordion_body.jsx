/**
 * Body for accordion.
 * @class ApAccordionBody
 */

'use strict'

import React, {PropTypes as types} from 'react'
import classnames from 'classnames'

/** @lends ApAccordionBody */
const ApAccordionBody = React.createClass({

  // --------------------
  // Specs
  // --------------------

  propTypes: {},

  mixins: [],

  statics: {},

  getInitialState () {
    const s = this
    return {}
  },

  getDefaultProps () {
    return {}
  },

  render () {
    const s = this
    let { props } = s

    return (
      <div className={ classnames('ap-accordion-body', props.className) }>
        { props.children }
      </div>
    )
  }
})

export default ApAccordionBody
