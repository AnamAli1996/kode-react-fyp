/**
 * Test case for ap-radio.
 * Runs with mocha.
 */
'use strict'

import ApRadio from '../lib/ap_radio'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('ap-radio', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <ApRadio/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
