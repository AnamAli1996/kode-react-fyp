/**
 * List bar message component.
 * @class ApListBarMessage
 */

'use strict'

import React, {PropTypes as types} from 'react'
import classnames from 'classnames'

/** @lends ApListBarMessage */
const ApListBarMessage = React.createClass({

  // --------------------
  // Specs
  // --------------------

  render () {
    const s = this
    let { state, props } = s

    return (
      <span className={ classnames('ap-list-bar-message', props.className) }
            style={ Object.assign({}, props.style) }>
		        { props.children }
            </span>
    )
  }

  // --------------------
  // Lifecycle
  // --------------------

  // ------------------
  // Custom
  // ------------------

  // ------------------
  // Private
  // ------------------
})

export default ApListBarMessage;
