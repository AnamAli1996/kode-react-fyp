/**
 * Test case for ap-radio-group.
 * Runs with mocha.
 */
'use strict'

import ApRadioGroup from '../lib/ap_radio_group'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('ap-radio-group', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <ApRadioGroup/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
