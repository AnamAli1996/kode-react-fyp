/**
 * Test case for ap-tag-style.
 * Runs with mocha.
 */
'use strict'

import ApTagStyle from '../lib/ap_tag_style'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('ap-tag-style', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <ApTagStyle/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
