/**
 * Test case for ap-radio-style.
 * Runs with mocha.
 */
'use strict'

import ApRadioStyle from '../lib/ap_radio_style'
import React from 'react'
import assert from 'assert'
import { shallow } from 'enzyme'

describe('ap-radio-style', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = shallow(
      <ApRadioStyle/>
    )
    assert.ok(element)
  })
})

/* global describe, before, after, it */
