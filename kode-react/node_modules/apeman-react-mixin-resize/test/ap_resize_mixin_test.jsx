/**
 * Test case for apResizeMixin.
 * Runs with mocha.
 */
'use strict'

import ApResizeMixin from '../lib/ap_resize_mixin'
import React from 'react'
import ReactDOM from 'react-dom/server'

import assert from 'assert'

describe('ap-resize-mixin', function () {
  this.timeout(3000)

  before(() => {

  })

  after(() => {

  })

  it('Demo mixin', () => {
    const MockClass = React.createClass({
      mixins: [
        ApResizeMixin
      ],
      render () {
        const s = this
        return React.createElement('div', {})
      },
      componentWillMount () {
        const s = this
        assert.ok(s.$apResizeMixin, true)
      }
    })
    let elm = React.createElement(MockClass, {
      windowDidResize () {
      }
    })
    let html = ReactDOM.renderToString(elm)
    assert.ok(html)
  })
})

/* global describe, before, after, it */
